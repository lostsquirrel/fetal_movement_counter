name: fetal_movement_counter
description: A local fetal moverment counter.


publish_to: 'none' 

version: 1.0.0+4

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  package_info_plus: ^3.0.2
  sqflite: ^2.2.4+1
  path: ^1.8.2
  path_provider: ^2.0.12
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true